# Environment configuration for the frontend

# The base URL for the FastAPI backend
# During local development with the backend, this might be http://localhost:8000/api
VITE_API_BASE_URL=http://localhost:8000/api
